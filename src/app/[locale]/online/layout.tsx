import "@/styles/vendor/plyr.css";

import type { ReactNode } from "react";

type OnlineLayoutProps = {
  children: ReactNode;
};

export default function OnlineLayout({ children }: OnlineLayoutProps) {
  return children;
}
